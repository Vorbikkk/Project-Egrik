import React from 'react';
import cl from './Loading.module.css'

const Loading = () => {

    return (
        <div className={cl.dashed_loader}>
            
        </div>
    );
};

export default Loading;